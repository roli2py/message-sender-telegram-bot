- [ ] Remove articles from tests' method names
- [ ] Replace a standard `patch` decorator to `pytest-mock`'s `mocker` fixture
- [x] Rename `spec` attributes to `autospec` and set `True` to them
- [x] Make unittests for handlers
- [ ] Make unittests for helpers
